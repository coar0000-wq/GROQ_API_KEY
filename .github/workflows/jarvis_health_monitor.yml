name: JARVIS Health Monitor

on:
  schedule:
    # 30ë¶?ê°„ê²© (???ˆì •??
    - cron: '0 * * * *'
    - cron: '30 * * * *'
  workflow_dispatch:

jobs:
  health_check_and_recovery:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: ?“¥ ?€?¥ì†Œ ì²´í¬?„ì›ƒ
        uses: actions/checkout@v4.1.7

      - name: ? Python 3.10 ?¤ì •
        uses: actions/setup-python@v5.2
        with:
          python-version: '3.10'

      - name: ?“¦ ?„ìˆ˜ ?¼ì´ë¸ŒëŸ¬ë¦??¤ì¹˜
        run: |
          pip install requests --break-system-packages
          echo "???¼ì´ë¸ŒëŸ¬ë¦??¤ì¹˜ ?„ë£Œ"

      - name: ?” JARVIS ?íƒœ ì²´í¬
        run: |
          echo "?” JARVIS ?œìŠ¤???íƒœ ì²´í¬ ?œì‘..."
          echo "???¤í–‰ ?œê°„: $(date '+%Y-%m-%d %H:%M:%S')"
          python scripts/health_check.py > health_check.log 2>&1
          cat health_check.log

      - name: ?”§ ?ë™ ë³µêµ¬ ?¤í–‰
        run: |
          echo "?”§ JARVIS ?ë™ ë³µêµ¬ ?œì‘..."
          python scripts/auto_recovery.py
          echo "??ë³µêµ¬ ?„ë£Œ"

      - name: ?“ ?¤í–‰ ?íƒœ ê¸°ë¡
        run: |
          git config --local user.email "jarvis-health@automation.local"
          git config --local user.name "JARVIS Health Monitor"

          echo "??GitHub Actions ?¤í–‰: $(date '+%Y-%m-%d %H:%M:%S')" >> .github/health_check.log

          git add -A

          if git diff --cached --quiet; then
            echo "?¹ï¸ ë³€ê²½ì‚¬???†ìŒ"
          else
            git commit -m "?¤– JARVIS ?ë™???¤í–‰ ($(date '+%Y-%m-%d %H:%M:%S'))"
            git push origin main
          fi

      - name: ?“Š ?¤í–‰ ê²°ê³¼
        run: |
          echo "=================================================="
          echo "??JARVIS 10ë¶??ë™ ?ˆì •???„ë£Œ"
          echo "=================================================="
          echo "???¤í–‰ ?œê°„: $(date '+%Y-%m-%d %H:%M:%S')"
          echo ""
          echo "?“‹ ì²´í¬ ??ª©:"
          echo "  ???°ì´???Œì¼ ? ì„ ??
          echo "  ???Œí¬?Œë¡œ???íƒœ"
          echo "  ???¤í¬ë¦½íŠ¸ ?Œì¼ ë¬´ê²°??
          echo "  ???ëŸ¬ ?ë™ ë³µêµ¬"
          echo ""
          echo "?”„ ?¤ìŒ ?¤í–‰: ??10ë¶???
          echo "=================================================="
