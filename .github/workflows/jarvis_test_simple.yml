name: ?§ª JARVIS ê°„ë‹¨ ?ŒìŠ¤??(?˜ë™ ?¤í–‰ë§?

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  simple_test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.1.7

      - name: Python ?¤ì •
        uses: actions/setup-python@v5.2
        with:
          python-version: '3.10'

      - name: ?„ìˆ˜ ?¼ì´ë¸ŒëŸ¬ë¦??¤ì¹˜
        run: |
          pip install requests feedparser --break-system-packages
          echo "???¼ì´ë¸ŒëŸ¬ë¦??¤ì¹˜ ?„ë£Œ"

      - name: ?“ ?°ì´???”ë ‰? ë¦¬ ?•ì¸
        run: |
          ls -la data/ 2>&1 || echo "??data ?´ë” ?†ìŒ"
          if [ -f "data/jarvis_work_detailed_log.json" ]; then
            echo "??ë¡œê·¸ ?Œì¼ ì¡´ì¬"
            head -30 data/jarvis_work_detailed_log.json
          else
            echo "??ë¡œê·¸ ?Œì¼ ?†ìŒ"
          fi

      - name: ?“ ?ŒìŠ¤?? ?‘ì—… ê¸°ë¡?˜ê¸°
        run: |
          python scripts/record_task_result.py "GitHub Actions ?ŒìŠ¤?? "completed" 5 10

      - name: ?“Š ê¸°ë¡ ?•ì¸
        run: |
          echo "=== ê¸°ë¡???°ì´??==="
          cat data/jarvis_work_detailed_log.json | python -m json.tool | head -50

      - name: ?“¤ ê²°ê³¼ ì»¤ë°‹
        run: |
          git config --local user.email "jarvis@test.local"
          git config --local user.name "JARVIS Test"
          git add data/jarvis_work_detailed_log.json
          git commit -m "?§ª ?ŒìŠ¤?? ?‘ì—… ê¸°ë¡ ê²€ì¦? || echo "ë³€ê²½ì‚¬???†ìŒ"
          git push origin main || echo "?¸ì‹œ ?¤íŒ¨"

      - name: ???„ë£Œ
        run: |
          echo "?ŒìŠ¤???„ë£Œ!"
          echo "?€?œë³´?œì—???¤ì‹œê°„ìœ¼ë¡??…ë°?´íŠ¸ ?•ì¸: https://coar0000.github.io/jarvis-luna/"
