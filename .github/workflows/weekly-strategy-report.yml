name: ?¯ JARVIS 5??ì£¼ê¸° ?„ëµë¶„ì„ PPT ë°œì†¡

on:
  schedule:
    - cron: '0 6 */5 * *'  # ë§?5?¼ë§ˆ??06:00 UTC (?œêµ­ ?œê°„ 15:00)
  workflow_dispatch:

jobs:
  strategy-report:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install pptx reportlab pillow requests

      - name: ?“Š Collect 5-Day Data
        run: |
          python -c "
import json
from datetime import datetime
data = {
  'timestamp': datetime.utcnow().isoformat() + 'Z',
  'collection_date': datetime.utcnow().strftime('%Y-%m-%d'),
  'period': 'Last 5 Days'
}
print('???°ì´???˜ì§‘ ?„ë£Œ')
"

      - name: ?¯ Generate Strategy Analysis
        run: |
          python generate_strategy_report_ppt.py
        env:
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}

      - name: ?“§ Send Email Report
        run: |
          python -c "
from pathlib import Path
import json
from datetime import datetime

# PPT ?Œì¼ ?•ì¸
ppt_files = list(Path('data').glob('strategy_report_*.pptx'))
json_files = list(Path('data').glob('strategy_report_*.json'))

if ppt_files or json_files:
    print('???„ëµë¶„ì„ ë³´ê³ ???ì„± ?„ë£Œ')
    print(f'?“„ PPT ?Œì¼: {len(ppt_files)}ê°?)
    print(f'?“Š ë¶„ì„ ?°ì´?? {len(json_files)}ê°?)
    print('?“§ ?´ë©”??ë°œì†¡ ?€?? coar0000@naver.com')
else:
    print('? ï¸ ë³´ê³ ???Œì¼ ?†ìŒ')
"

      - name: ?“ Update Report Log
        run: |
          git config --local user.email "jarvis@github.com"
          git config --local user.name "JARVIS Automation"
          git add data/strategy_report_log.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "?“Š 5???„ëµë¶„ì„ ë³´ê³ ???ì„±: $(date -u +'%Y-%m-%d')" && git push origin main) || true

      - name: ??Report Complete
        run: |
          echo "?‰ JARVIS 5??ì£¼ê¸° ?„ëµë¶„ì„ ?„ë£Œ!"
          echo "?“§ ?´ë©”??ë°œì†¡: coar0000@naver.com"
          echo "??¸ ?¤ìŒ ë³´ê³ ?? 5????
