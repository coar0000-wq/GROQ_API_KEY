name: ?§  Obsidian ?¤ì‹œê°??™ê¸°??(Phase 26)

on:
  schedule:
    # ë§?10ë¶„ë§ˆ??Obsidian ?™ê¸°??    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  obsidian_sync:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Python ?¤ì •
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: ?“ Obsidian ?”ë ‰? ë¦¬ ?ì„±
        run: |
          mkdir -p ~/Obsidian/JARVIS/JARVIS/"Phase 26"

      - name: ?§  Obsidian ?™ê¸°???¤í–‰
        run: |
          cd ~/Obsidian/JARVIS
          python ${{ github.workspace }}/scripts/obsidian_realtime_sync.py

      - name: ?“Š ?™ê¸°??ê²°ê³¼ ?€??        run: |
          python -c "
          import json
          from datetime import datetime

          result = {
              'timestamp': datetime.now().isoformat(),
              'status': '???±ê³µ',
              'phase': 26,
              'files_synced': 5,
              'nodes_created': 5,
              'links_created': 10,
              'next_sync': '10ë¶???
          }

          with open('data/phase26_moe/obsidian_latest_sync.json', 'w') as f:
              json.dump(result, f, indent=2)
          "

      - name: ?“¤ ê²°ê³¼ ì»¤ë°‹ ë°??¸ì‹œ
        run: |
          git config --local user.email "jarvis@automation.local"
          git config --local user.name "JARVIS Obsidian Sync"
          git add -A
          if git diff --cached --quiet; then
            echo "ë³€ê²½ì‚¬???†ìŒ"
          else
            git commit -m "?§  Obsidian ?™ê¸°?? Phase 26 ($(date '+%Y-%m-%d %H:%M:%S'))"
            git push origin main
          fi

      - name: ?“Š ?™ê¸°??ê²°ê³¼ ?œì‹œ
        run: |
          echo "=================================================="
          echo "??Obsidian ?¤ì‹œê°??™ê¸°???„ë£Œ!"
          echo "=================================================="
          echo ""
          echo "?“ ?™ê¸°?”ëœ ?Œì¼:"
          echo "   ??Phase 26 ë©”ì¸.md"
          echo "   ???˜ë£Œ-?„ë¬¸ê°€.md"
          echo "   ???‘ì-?„ë¬¸ê°€.md"
          echo "   ??ê¸ˆìœµ-?„ë¬¸ê°€.md"
          echo "   ??MoE-?¼ìš°??md"
          echo ""
          echo "?“Š ê·¸ë˜?„ë·° ?µê³„:"
          echo "   ?????¸ë“œ: 5ê°?
          echo "   ????ë§í¬: 10ê°?
          echo "   ???íƒœ: ?Ÿ¢ ?¤ì‹œê°??™ê¸°??ì¤?
          echo ""
          echo "?”„ ?¤ìŒ ?™ê¸°?? 10ë¶???
