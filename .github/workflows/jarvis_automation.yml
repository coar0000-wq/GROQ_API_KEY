name: ?¤– JARVIS ?ë™???œìŠ¤??(10ë¶„ë§ˆ??

on:
  schedule:
    # ë§?10ë¶„ë§ˆ???¤í–‰
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  jarvis_automation:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Python ?¤ì •
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: ?„ìˆ˜ ?¼ì´ë¸ŒëŸ¬ë¦??¤ì¹˜
        run: |
          pip install requests feedparser --break-system-packages

      - name: ?¤– JARVIS ?ë™ ?‘ì—… ?¤í–‰
        run: |
          python scripts/jarvis_automation_real.py

      - name: ?“Š ê²°ê³¼ ì»¤ë°‹ ë°??¸ì‹œ
        run: |
          git config --local user.email "jarvis@automation.local"
          git config --local user.name "JARVIS Automation"
          git add -A
          if git diff --cached --quiet; then
            echo "ë³€ê²½ì‚¬???†ìŒ"
          else
            git commit -m "?¤– JARVIS ?ë™ ?¤í–‰: $(date '+%Y-%m-%d %H:%M:%S')"
            git push origin main
          fi
