name: ?¤– JARVIS ?ë™ ì§„í™”

on:
  schedule:
    # ë§?1ë¶„ë§ˆ???¤í–‰ (UTC ?œê°„)
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  auto-evolution:
    runs-on: ubuntu-latest
    steps:
      - name: ?“¦ ì½”ë“œ ì²´í¬?„ì›ƒ
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: ? Python ?¤ì •
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: ?? JARVIS ?ë™ ì§„í™” ?œì‘
        run: python .github/workflows/update_activity_log.py

      - name: ?“ ì»¤ë°‹ ë°??¸ì‹œ
        run: |
          git config user.name "JARVIS"
          git config user.email "jarvis@automated.local"
          git add data/
          git commit -m "?¤– JARVIS ?ë™ ì§„í™” - $(date +'%Y-%m-%d %H:%M:%S')" || true
          git push origin main || true
