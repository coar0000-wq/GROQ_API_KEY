name: ?¤– JARVIS Auto Update Tasks

# ??ë§?1ë¶„ë§ˆ???¤í–‰
on:
  schedule:
    - cron: '* * * * *'  # ë§?ë¶??¤í–‰

  # ?˜ë™?¼ë¡œ???¤í–‰ ê°€??  workflow_dispatch:

jobs:
  update-tasks:
    runs-on: ubuntu-latest

    steps:
      # ?“¦ ?€?¥ì†Œ ì²´í¬?„ì›ƒ
      - name: Checkout Repository
        uses: actions/checkout@v3

      # ? Python ?¤ì •
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # ?“¦ ?„ìˆ˜ ?¼ì´ë¸ŒëŸ¬ë¦??¤ì¹˜
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ?? ?œë™ ë¡œê·¸ ?…ë°?´íŠ¸ ?¤í–‰ (?¤ì œ ?°ì´???˜ì§‘)
      - name: Run JARVIS Activity Log Update
        run: python update_activity_log.py

      # ?“ ë³€ê²½ì‚¬??ì»¤ë°‹ ë°??¸ì‹œ
      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "JARVIS Automation"

          if git diff-index --quiet HEAD --; then
            echo "??No changes to commit"
          else
            git add data/activity_log.json
            git commit -m "?¤– [JARVIS] ?¤ì œ ?°ì´???˜ì§‘ ?„ë£Œ - $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
            git push
            echo "??Changes pushed to repository"
          fi

      # ?“Š ?„ë£Œ ë¡œê·¸
      - name: Log Completion
        run: |
          echo "?‰ JARVIS ?ë™ ?…ë°?´íŠ¸ ?„ë£Œ!"
          echo "???¤í–‰ ?œê°„: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "?“ GitHub Actions: https://github.com/${{ github.repository }}/actions"
