name: ?¤– JARVIS Core Automation (ë§?10ë¶?

on:
  schedule:
    # ë§?10ë¶„ë§ˆ???¤í–‰ (?µí•© ?ë™??
    - cron: '*/10 * * * *'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  core-automation:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4.1.7
        with:
          fetch-depth: 0

      - name: ? Python 3.10 ?¤ì •
        uses: actions/setup-python@v5.2
        with:
          python-version: '3.10'

      - name: ?“¦ ?¼ì´ë¸ŒëŸ¬ë¦??¤ì¹˜
        run: |
          pip install requests feedparser torch numpy matplotlib --break-system-packages

      # ============================================================
      # [1/4] ?¤ì´???í’ˆ ?ë™ ë°œêµ´ (ë§?10ë¶?
      # ============================================================

      - name: ?›ï¸?[1/4] ?¤ì´???í’ˆ ë°œêµ´
        id: daiso_discovery
        run: |
          echo "?›ï¸??¤ì´???í’ˆ ?ë™ ë°œêµ´..."
          if python daiso_product_discovery.py; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failed" >> $GITHUB_OUTPUT
          fi

      # ============================================================
      # [2/4] Obsidian ?¤ì‹œê°??™ê¸°??(ë§?10ë¶?
      # ============================================================

      - name: ?“š [2/4] Obsidian ?™ê¸°??        id: obsidian_sync
        run: |
          echo "?“š Obsidian ê·¸ë˜???™ê¸°??.."
          if python scripts/obsidian_realtime_sync.py; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failed" >> $GITHUB_OUTPUT
          fi

      # ============================================================
      # [3/4] YouTube ?°ì´???˜ì§‘ (ë§?10ë¶?
      # ============================================================

      - name: ?¬ [3/4] YouTube ?°ì´???˜ì§‘
        id: youtube_collect
        run: |
          echo "?¬ YouTube ?œë¡­?¬í•‘ ë¶„ì„..."
          if python scripts/youtube_dropshipping_analysis.py; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failed" >> $GITHUB_OUTPUT
          fi

      # ============================================================
      # [4/4] Google ê²€???°ì´???˜ì§‘ (ë§?10ë¶?
      # ============================================================

      - name: ?” [4/4] Google ê²€???°ì´???˜ì§‘
        id: google_collect
        run: |
          echo "?” Google Trends ë¶„ì„..."
          if python scripts/google_search_data_collection.py; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failed" >> $GITHUB_OUTPUT
          fi

      # ============================================================
      # [5/5] Phase 26 ?¤ì‹œê°?ì§„í–‰???…ë°?´íŠ¸ (ë§?10ë¶?
      # ============================================================

      - name: ?§  [5/5] Phase 26 ì§„í–‰???…ë°?´íŠ¸
        id: phase_26_progress
        run: |
          echo "?§  Phase 26 MoE ?¼ìš°??ì§„í–‰???…ë°?´íŠ¸..."
          if python phase_26_progress_realtime.py; then
            echo "status=success" >> $GITHUB_OUTPUT
          else
            echo "status=failed" >> $GITHUB_OUTPUT
          fi

      # ============================================================
      # GitHub ?ë™ ?¸ì‹œ
      # ============================================================

      - name: ?“¤ GitHub ?ë™ ?¸ì‹œ
        run: |
          git config user.name "JARVIS"
          git config user.email "jarvis@automated.local"
          git add data/ index.html
          git commit -m "?¤– JARVIS Core: ?¤ì´??Obsidian+YouTube+Google (ë§?10ë¶? $(date '+%Y-%m-%d %H:%M:%S UTC'))" || echo "No changes to commit"
          git push origin main || echo "? ï¸ Push failed"

      # ============================================================
      # ?‘ì—… ?„ë£Œ ë¡œê·¸
      # ============================================================

      - name: ???„ë£Œ ë¡œê·¸
        if: always()
        run: |
          echo "?¯ JARVIS Core Automation ?„ë£Œ"
          echo "- ?¤ì´???í’ˆ ë°œêµ´: ${{ steps.daiso_discovery.outputs.status }}"
          echo "- Obsidian ?™ê¸°?? ${{ steps.obsidian_sync.outputs.status }}"
          echo "- YouTube ?˜ì§‘: ${{ steps.youtube_collect.outputs.status }}"
          echo "- Google ?˜ì§‘: ${{ steps.google_collect.outputs.status }}"
          echo "- Phase 26 ì§„í–‰?? ${{ steps.phase_26_progress.outputs.status }}"
